"""Steps used to construct a SpecHLA reference from an IMGT/HLA checkout.

Each module implements one stage of ``script/build_reference.py`` and is kept
importable on its own so the stages can be tested in isolation.
"""
